// import Link from 'next/link';

// export default function StoryCard({ story }) {
//   return (
//     <div className="story-card">
//       <img src={story.image} alt={story.title} />
//       <h3>{story.title}</h3>
//       <p>{story.description}</p>
//       <Link href={`/story/${story.id}`}>اقرأ القصة</Link>
//     </div>
//   );
// }
